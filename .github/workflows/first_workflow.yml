name: First Workflow

on: [push]

jobs:
    run-first-command:
        runs-on: ubuntu-latest
        steps:
            - name: Wyświetl komunikat
              run: echo "Komunikat"
              
